spring:
  #data:
    mongodb:
      host: mymongo         # MongoDB container name
      port: 27017
      database: admin     # Change to your DB name
      username: admin        # From MONGO_INITDB_ROOT_USERNAME
      password: Admin@123     # From MONGO_INITDB_ROOT_PASSWORD
      authentication-database: admin